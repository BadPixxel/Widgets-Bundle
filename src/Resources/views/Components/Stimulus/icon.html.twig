<button class="btn btn-xs px-1 small {{ class }}"
        data-action="live#action"
        data-live-action-param="{{ action }}"
        {% block content %}{% endblock %}
>
    <i class="{{ icon }}"></i>
</button>
