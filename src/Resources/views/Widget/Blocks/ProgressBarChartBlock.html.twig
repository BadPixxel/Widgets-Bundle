{% trans_default_domain "SplashWidgetsBundle" %}   
{% import "@SplashWidgets/Macros/widgets.html.twig" as Wid %}

{#
    Proggress Bar
#}
<div class="text-center">
    {% if Block.title is not empty %}
        <h5>
            {{ Wid.Text(Block.title,Block.Options) }}
        </h5>
    {% endif %}

    <div class="panel-body text-center p-2">
        {% if Block.Values is not empty %}
            {{ Wid.Progress( Block.Options.ChartOptions, Block.Values ) }}
        {% endif %}
    </div>

</div>

    