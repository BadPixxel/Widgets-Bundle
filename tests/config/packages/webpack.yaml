# app/config/webpack_encore.yaml
when@dev:
    webpack_encore:
        # The path where Encore is building the assets - i.e. Encore.setOutputPath()
        output_path: '%kernel.project_dir%/tests/public/build'
